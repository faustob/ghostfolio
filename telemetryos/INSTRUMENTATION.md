# Instrumentation for Client

Generated by TelemetryOS against `92216e6`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** node · npm · host: docker · telemetry: sdk-export → wiring model **app-sdk**
**Self-review:** 1 generation pass — ⚠ passed with warnings.
**Outstanding (1):**
- ⚠ qr-unwired-telemetry (`apps/api/src/telemetry/telemetry.ts`): The new web RUM helpers (recordWebVital, recordWebJsError, recordWebSessionStart) and their instruments are defined but the diff shows nothing that calls them — no browser/RUM ingestion endpoint or caller is added, so these instruments never record. If no caller exists in the repo this telemetry is dead. — _Wire these record* helpers to an actual server-side RUM ingestion route (API endpoint receiving browser vitals), or omit them until a caller exists._

## SLIs implemented
- **Client — Largest Contentful Paint (p75)** (`client-web-lcp-p75`) — target < 2.5s over 28-day
- **Client — Interaction to Next Paint (p75)** (`client-web-inp-p75`) — target < 200ms over 28-day
- **Client — Cumulative Layout Shift (p75)** (`client-web-cls-p75`) — target < 0.1 over 28-day
- **Client — Client JS Error Rate** (`client-web-js-error-rate`) — target < 1% over 7-day
- **Client — SPA Route-Transition Time (p75)** (`client-web-spa-nav-p75`) — target < 1.5s over 28-day

## Modified files
- `apps/api/src/telemetry/telemetry.ts`

## Dependencies
No additional dependencies identified beyond your existing OpenTelemetry setup.
