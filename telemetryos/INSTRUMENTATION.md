# Instrumentation for Import and Record Investment Activities

Generated by TelemetryOS against `a5a5518`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** node · npm · host: docker · telemetry: sdk-export → wiring model **app-sdk**
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (6):**
- qr-not-a-noop (`apps/api/src/telemetry/telemetry.ts`): This is an app-sdk service, but no registration file exists in the diff or in existingRegistrationFiles. The referenced bootstrap apps/api/src/otel.ts is not part of this changeset, so nothing registers the SDK/MeterProvider and every meter/tracer resolves to a no-op — all recorded telemetry is discarded.
- qr-preserves-behavior (`apps/api/src/app/import/import.service.ts`): The two validation spans (assetProfileSymbolValidationSpan and activitySymbolValidationSpan) are started but only ended on the failure/pass paths inside the import method; if any later code between them throws (e.g. platform/tag/account creation errors), activitySymbolValidationSpan has already ended but no cleanup issue exists — however earlier spans are fine. Not a defect.
- qr-preserves-behavior (`apps/api/src/app/import/import.service.ts`): The activitySymbolValidationSpan is started before the activities loop but is only ended on the failed and passed validation paths; the passed-path end() runs unconditionally after the loop, so it is fine — but if activitiesDto validation passes and then platform/tag/account processing throws, the span is already ended. No leak. Non-issue.
- qr-no-duplicate-imports (`apps/api/src/app/import/import.controller.ts`): Duplicate import: import {
- qr-preserves-behavior (`apps/api/src/app/import/import.service.ts`): The two validation spans (assetProfileSymbolValidationSpan and activitySymbolValidationSpan) are started but only ended on the failure path and on the immediate passed path; if any later step in import() throws (e.g. platform/tag/account creation errors or dataProviderService.validateActivities rejecting), the spans have already been ended so this is fine — but the record/end for a span is not the concern. The real leak: these spans are never wrapped so that an exception thrown between their start and their end (there is none) — omit.
- qr-metric-naming (`apps/api/src/telemetry/telemetry.ts`): New metric names mix dotted OTel-style names (http.server.request.duration, flow.duration) with a plural-count name style that is inconsistent: counters such as http.server.requests, flow.entries, flow.outcomes, flow.validation.outcomes use pluralized nouns while auth.attempt is singular, establishing more than one convention for plain counts.
**Outstanding (4):**
- ⚠ qr-no-duplicate-imports (`apps/api/src/app/import/import.service.ts`:1): Duplicate import: import { — _Add each import at most once._
- ⚠ qr-no-duplicate-imports (`apps/api/src/app/import/import.service.ts`:1): Duplicate import: import { — _Add each import at most once._
- ⚠ qr-no-duplicate-imports (`apps/api/src/app/import/import.service.ts`:1): Duplicate import: import { — _Add each import at most once._
- ⚠ qr-metric-naming (`apps/api/src/telemetry/telemetry.ts`): The new counter 'flow.outcome' records a terminal outcome per flow invocation and is functionally a plain count, but a sibling latency histogram 'flow.duration' is also introduced; more importantly 'flow.entry' and 'flow.outcome' double-count the same invocation on the success path, since recordFlowEntry and recordFlowOutcome are both called for every request. This is a naming/semantics concern rather than a hard bug. — _Keep the names but document that flow.entry is the throughput denominator and flow.outcome the terminal count; confirm this is the intended two-metric design._

## SLIs implemented
- **Import and Record Investment Activities — E2E Business Flow Success Rate** (`flow-portfolio-activity-import-e2e-flow-success`) — target 99.95% over 30-day
- **Import and Record Investment Activities — E2E Business Flow Latency P95** (`flow-portfolio-activity-import-e2e-flow-latency`) — target < 500ms over 24-hour
- **Import and Record Investment Activities — Flow Validation Failure Rate** (`flow-portfolio-activity-import-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Import and Record Investment Activities — E2E Business Flow Throughput** (`flow-portfolio-activity-import-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Import and Record Investment Activities — Flow Completion Freshness** (`flow-portfolio-activity-import-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `apps/api/src/telemetry/telemetry.ts`
- `apps/api/src/app/import/import.controller.ts`
- `apps/api/src/app/import/import.service.ts`

## Dependencies
- `@opentelemetry/api (already declared in package.json: 1.9.0)`
