# Instrumentation for Client

Generated by TelemetryOS against `a5a5518`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** node · npm · host: docker · telemetry: sdk-export → wiring model **app-sdk**
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (2):**
- qr-unwired-telemetry (`apps/api/src/app/endpoints/rum/rum.controller.ts`): The RumController is mounted at @Controller('rum') with no global 'api/v1' prefix visible, but the browser client beacons to '/api/v1/rum'. If the API sets a global prefix and versioning the route matches, but if not, the endpoint resolves to '/rum' and every RUM beacon 404s, so no web-vital/route-change/js-error metric is ever recorded.
- qr-metric-naming (`apps/api/src/telemetry/telemetry.ts`): New metric names mix two separator/prefix conventions against the semconv-style corpus: http.server.* names use dot.separated segments while attributes/counters like 'web.js.errors', 'auth.attempt', and 'browser.route_change.duration' invent domain prefixes inconsistently, and 'http.server.requests' pluralizes a count where semconv uses http.server.request.* singular.
**Outstanding (3):**
- ⚠ qr-no-duplicate-imports (`apps/client/src/app/app.component.ts`:1): Duplicate import: import { — _Add each import at most once._
- ⚠ qr-no-duplicate-imports (`apps/client/src/app/app.component.ts`:1): Duplicate import: import { — _Add each import at most once._
- ⚠ qr-not-a-noop (`apps/api/src/app/endpoints/rum/rum.controller.ts`:73): sanitizeDeviceType/sanitizeErrorType are declared to return string but Set.has(deviceType) where deviceType?: string passes a possibly-undefined value, and the narrowed value is returned as-is; this is a type mismatch the compiler gate will surface. — _Guard against undefined before the Set.has check (e.g. `deviceType && DEVICE_CLASSES.has(deviceType) ? deviceType : 'unknown'`)._

## SLIs implemented
- **Client — Largest Contentful Paint (p75)** (`client-web-lcp-p75`) — target < 2.5s over 28-day
- **Client — Interaction to Next Paint (p75)** (`client-web-inp-p75`) — target < 200ms over 28-day
- **Client — Cumulative Layout Shift (p75)** (`client-web-cls-p75`) — target < 0.1 over 28-day
- **Client — Client JS Error Rate** (`client-web-js-error-rate`) — target < 1% over 7-day
- **Client — SPA Route-Transition Time (p75)** (`client-web-spa-nav-p75`) — target < 1.5s over 28-day

## Modified files
- `apps/api/src/telemetry/telemetry.ts`
- `apps/api/src/app/app.module.ts`
- `apps/client/src/app/app.component.ts`
- `package.json`

## New files
- `apps/api/src/app/endpoints/rum/rum.controller.ts`
- `apps/api/src/app/endpoints/rum/rum.module.ts`
- `apps/client/src/app/services/web-vitals.service.ts`

## Dependencies
- `web-vitals@5.1.0 (browser RUM collection in apps/client)`
- `@opentelemetry/api@1.9.0 (already present — used by apps/api/src/telemetry/telemetry.ts which records the RUM metrics)`
